DynamicIslandCore — System Aperture experiment

Core path:
NCNotificationDispatcher -> postNotificationWithRequest:
-> DXNotificationElement -> SBSystemApertureViewController
-> registerElement:

Target: jailbroken iOS 17.x / SpringBoard.
This is a minimal experimental skeleton, not a 1:1 DynamicX clone.
